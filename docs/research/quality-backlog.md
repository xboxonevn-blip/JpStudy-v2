# Quality Backlog

Continuous detect -> log -> prioritize -> fix -> verify -> commit loop.

## 2026-05-17 Session

| ID | Track | Severity | Repro / Detection | Status | Verification |
| --- | --- | --- | --- | --- | --- |
| QA-A-001 | App defects | P0 | Live retest: click `Hồ sơ` from shell after visiting `/#/exam-center`; observed `/#/vocab` and `Học` highlighted. | Fixed locally | Added VI route smoke test; `flutter test test/app/navigation/app_shell_scaffold_test.dart test/app/navigation/app_route_smoke_test.dart` passed. Live proof pending after deploy. |
| QA-A-002 | App defects | P1 | Live retest: VI UI leaked `Ready now`, `Companion`, `14 chapter`, `Catalog`, `review`; post-deploy sweep still found `Companion` in Minna status badges. | Fixed locally | Added widget regression for Vietnamese vocab catalog badges and duplicate `Bổ trợ` guard; focused vocab test passed. Live proof pending after deploy. |
| QA-A-003 | App defects | P1 | Live retest: Review page used warehouse/logistics metaphors: `Chặn hàng review trước`, `Dọn hàng kanji`, `hàng đợi đang mở`. | Fixed locally | Added provider regression for learner-facing VI review copy; focused practice tests passed. Live proof pending after deploy. |
| QA-A-004 | App defects | P0 | Live retest: `/#/lesson/1` at N2 showed `N2 / Minna No Nihongo 1`; N3/N2/N1 should use Shin Kanzen. | Fixed locally | Added repository regression for prefixed legacy Minna title; lesson repository focused test passed. Live proof pending after deploy. |
| QA-A-005 | App defects | P1 | Live post-deploy check: Review page no longer had old warehouse copy, but the next-lesson action still showed `Bắt đầu Minna No Nihongo 200001` for N2. | Fixed locally | Added `continueLessonLabelForTesting` regression; `flutter test test/features/home/continue_provider_test.dart` and practice board provider tests passed. Live proof pending after deploy. |
| QA-A-006 | App defects | P0 | Owner live repro: Học -> Shin Kanzen Master N3/N2/N1 vocab tracks opened empty cards with `0`; Minna/Hajimete loaded. Root cause: Shin Kanzen companion cards fell through to `/vocab/review` instead of a real catalog route. | Fixed + pushed `624ac24c` | `flutter analyze lib test`, `python tooling/audit_ui_string_literals.py --check`, taxonomy guard, and full `flutter test` passed. Deployed to Firebase Hosting. Live proof: N3 shows 25 lessons/404 terms with non-zero lesson rows; N2 shows 25 lessons/1797 terms; N1 shows 25 lessons/3476 terms. |
| QA-A-007 | App defects | P0 | Owner request: `Lộ trình theo giáo trình` is decorative. Stages/tags must only show if content exists and loads; listening must be hidden/coming-soon unless inventory exists; every tag must link to non-empty content; timeline must be realistic/adaptive; Month 1 load must sequence vocab -> grammar. | Fixed + pushed `b2df7ef8` | Roadmap uses resource objects with destinations, hides listening resources, replaces fixed month labels with hour/adaptive labels, and sequences upper levels vocab -> grammar -> reading/kanji -> mock. `flutter analyze lib test`, string guard, taxonomy guard, focused roadmap tests, and full `flutter test` passed. Deployed; cache-disabled live proof for N3: all visible chips opened non-empty routes. Full N5/N4/N2/N1 chip-by-chip live sweep still pending. |
| QA-A-008 | App feature | P1 | Owner-approved: grammar-point detail screens must replace manual `Đánh dấu đã học` with a practice gate across N5-N1; pass >=4/5 auto-marks `Đã hiểu`, seeds grammar SRS, logs mistakes, and uses one shared grammar-practice bank. | Partial fixed locally | First TDD slice removes the manual mark button from grammar detail, shows `In progress` / `Understood ✓` status, opens a 5-question practice gate, and auto-marks the point learned after a >=4/5 pass while existing per-answer grammar SRS/mistake logging remains in the shared practice screen. Verified: focused grammar tests, `flutter analyze lib test`, `python tooling\audit_ui_string_literals.py --check`, taxonomy guard, and full `flutter test` (`2335`). Deployed to Firebase Hosting; live proof with fresh EN/N5 browser: `/#/grammar/1` showed `In progress` + `Practice check`, and clicking it opened `Practice check` with `Question 1 of 5`; console warnings/errors `0`. Second TDD slice routes generated questions through `GrammarPracticeBank`, registers `grammarPractice` in `index.json`, adds an empty authored-bank manifest with consumer routes, and adds guards that every seeded N5-N1 grammar point has at least one generated question and authored questions cannot be orphaned. Third TDD slice routes JLPT mock grammar through the same bank instead of local `meaning/pattern` builders. Still pending: authored v2 question content and broader shared quiz UI consolidation from QA-A-009. |
| QA-A-009 | App feature | P1 | Owner-approved: quiz/answer-selection UX must fit question + all 4 answers in one viewport, remove repeated config chrome, use select -> confirm, and become the single shared quiz component for lesson tests, grammar gate, and JLPT mock/exam. | Fixed + pushed `33d65e3a`, `ea48dd17` | Grammar multiple-choice now uses select -> confirm, compact options, desktop 2x2 grid, and no-scroll mobile widget guard; grammar practice screen no longer repeats the full mode/config card per question. Learn/test multiple-choice now also uses select -> confirm. Mobile TestScreen has no-overflow guards for readable header, visible MC confirm, visible True/False choices, and reliable compact detection in the mobile shell. Added `SharedAnswerSelection` and routed grammar gate, lesson/test MC, and JLPT Mock Pro answers through it; JLPT Mock Pro now selects first and only commits after `Trả lời`. Added compact JLPT Mock Pro active layout so mobile keeps all four answer choices and the confirm button hit-testable in a constrained viewport. Verified locally with focused quiz suites, string guard, taxonomy guard, content status report, `npm run test:research-tooling`, `flutter analyze lib test`, and full `flutter test` (`2340`). Deployed to Firebase Hosting; live proof: desktop JLPT Mock Pro kept progress at `0/18 answered` after selecting and changed to `1/18 answered` only after `Answer`; mobile `390x640` showed all four choices plus `Answer`, commit changed `0/18` to `1/18`, console warnings/errors `0`. |
| QA-A-010 | App defects | P0 | Owner live bug: `Quy tắc Hán Việt` is routed through the Foundations/Kana branch, so at N3 it shows `Kana không khả dụng ở cấp N3`. Hán-Việt rules must be reachable from Kanji at every JLPT level and visible only when UI language is Vietnamese. | Fixed + pushed `baad16f5` | Added `/kanji/han-viet`, removed the N5-only gate from the legacy Hán-Việt path, hid Hán-Việt entry points for EN/JA, and routed Kanji hub CTA to the Kanji-owned path. Focused tests, `flutter analyze lib test`, string guard, taxonomy guard, and full `flutter test` passed. Deployed; cache-disabled live proof: N3 `/kanji/han-viet` and `/foundations/han-viet` render rules without Kana lock, and EN Kanji hides the Hán-Việt action. |
| QA-A-011 | App feature | P1 | Owner-approved: kanji detail/grid/lesson/practice/search UX must switch by UI language: Vietnamese = Hán-Việt-centric, English = English/radical mnemonics and romaji with all Hán-Việt hidden, Japanese = Japanese immersion with Hán-Việt hidden. | Partial fixed + pushed `a3648697` | Kanji detail now shows Hán-Việt row, Vietnamese mnemonic, and inline Hán-Việt panel only for VI; EN/JA hide Hán-Việt aids. Kanji card semantics now switch by UI language. Fixed content DB seed/migration v33 so `labels.hanViet` reaches detail metadata. Search uses Hán-Việt query keywords only in VI. Lesson kanji list now hides Hán-Việt labels and Vietnamese component names outside VI, and kanji reading/write practice uses non-Vietnamese meaning fallback for EN/JA. Added `meaningJa` model/DB/seed/repository plumbing so Japanese definitions can render wherever data supplies `labels.meaningJa`; current assets have no `meaningJa` entries, so live JA remains on English fallback until the source-backed kanji data expansion lands. Verified locally with focused Kanji tests, `flutter analyze lib test`, UI string guard, taxonomy guard, and full `flutter test` (`2321`). Deployed `a3648697`; live proof: JA Kanji hub loads with Japanese chrome and English fallback meanings, and no source-backed Japanese definitions are claimed. Still pending: Japanese definition data completeness. |
| QA-A-012 | App defects | P0 | Live QA while verifying QA-A-011: clicking `書く` from `/#/kanji/practice` blanked the content area; console showed `RangeError: max must be in range ... was 0` from handwriting random seed generation on web. | Fixed + pushed `b07d10f6` | Root cause: `Random().nextInt(1 << 32)` is web-unsafe after JS compilation. Added a regression guard and changed the seed max to a web-safe constant. Verified locally: RED guard failed before the fix, then focused handwriting tests, `flutter analyze lib test`, UI string guard, taxonomy guard, and full `flutter test` passed. Built/deployed `b07d10f6`; live proof: Japanese `/#/kanji/practice` -> `書く` renders `手書き: N3 — 新しい漢字` with English fallback meaning and no RangeError. |
| QA-A-013 | App defects | P0 | Owner live repro: VI/N3 `/#/kanji` showed `Chưa tải được dữ liệu kanji`; `/#/kanji/practice` -> `Viết` showed `Tải dữ liệu thất bại`. Likely regression from content DB schema v34 `meaning_ja` migration for existing users. | Fixed + pushed `a8ae956c` | Root cause: an existing content DB could report current `user_version` while physically missing `kanji.meaning_ja`, so Drift selects/seeds failed with `table kanji has no column named meaning_ja`. Added physical-column self-heal, v35 stale metadata reseed, and DB-backed `content_meta.kanjiSeedRevision` so current-version browsers get post-v35 Kanji asset metadata updates. Verified locally: focused DB/repository tests, `flutter analyze lib test`, UI string guard, taxonomy guard, content status report, and full `flutter test` passed with `2329` tests. Built/deployed `a8ae956c`; live proof: VI/EN/JA across N5-N1 loaded Kanji grid rows and `Write/Viết/書く` handwriting practice with zero console errors, and existing-browser N3 `将` detail updated to `tướng, tương lai` while write practice loaded. |
| QA-A-014 | App defects | P0 | Owner live repro after QA-A-013: VI/N3 `/#/kanji` and `/#/kanji/practice` could still fail for existing browsers. Fresh live storage loaded N3 grid correctly, pointing to persisted content DB repair rather than asset absence. | Fixed + pushed `78febd31` | Root cause: kanji DB self-heal treated any level with `COUNT(*) > 0` as seeded, so a current-version but partial/stale IndexedDB was never repaired. Added a RED regression for a current-version N3 DB with one row and `content_meta.kanjiSeedRevision=16`, then made startup compare per-level kanji counts against `assets/data/content/index.json` and reseed incomplete levels. Verified locally with focused native DB/reachability tests, Chrome reachability test, string guard, taxonomy guard, content status report, `flutter analyze lib test`, and full `flutter test` (`2330`). Built/deployed `78febd31`; cache-disabled live proof: VI grids N5-N1 all loaded non-empty, VI write loaded, EN grid/write loaded, JA grid/write loaded, console warnings/errors `0`. |
| QA-A-015 | App defects | P0 | Follow-up live proof after lesson-17 source verification: fresh browser showed correct `技`, but an existing live browser could keep stale Kanji metadata. Local reachability test also hung after runtime ensure was added. | Fixed + pushed `833ed3c8` | Root cause: `LessonRepository` called public `ContentDatabase.ensureKanjiContentCurrent()` before the first content DB query; opening Drift then ran `beforeOpen`, which awaited the same pending public ensure and deadlocked. The repo also cleared Kanji caches on every read. Fixed `beforeOpen` to use the private ensure path, made public ensure return whether content was repaired, and made the repository ensure once per lifecycle while clearing caches only when needed. Verified: focused DB lazy-seed tests, Kanji runtime reachability test, coverage/taxonomy subset, `flutter analyze lib test`, UI string guard, and node research tests passed. Built/deployed; live proof: VI N3 grid loaded `203`, VI `Viết` loaded real Kanji (`将`), EN grid loaded, JA grid loaded, and JA radicals `214` loaded. |
| QA-A-016 | App defects | P0 | N2 lesson-03 live proof after deploy still showed stale `圧 (compression, condensation, pressure)` even though Hosting served the updated lesson JSON. | Fixed + pushed `dff7a998` | Root cause: non-fingerprinted Flutter shell/content files had `public, max-age=86400`, so browsers could run an old `main.dart.js` without the latest Kanji seed sentinel. Changed `main.dart.js`, `flutter_bootstrap.js`, `flutter.js`, `AssetManifest*`, and content JSON to `Cache-Control: no-cache`; kept `sqlite3.wasm` and `drift_worker.js` on `public, max-age=2592000`. Verified `npm run test:research-tooling`, built/deployed, live headers returned `main.dart.js=no-cache`, content JSON `no-cache`, `sqlite3.wasm=max-age=2592000`; normal reload fetched `main.dart.js` from network and VI/N2 search `圧` opened `Áp (áp lực; nén; ép)` with console warnings/errors `0`. |
| QA-A-017 | App defects | P0 | Owner audit: later Hosting cache commits restored `public, max-age=86400` for un-fingerprinted Flutter shell/content assets, making recent Kanji/content fixes invisible to returning users for up to 24h. | Fixed + pushed `feeaca64`; rechecked 2026-05-19 | Restored QA-A-016 policy: `main.dart.js`, `flutter_bootstrap.js`, `flutter.js`, `assets/AssetManifest*`, and `assets/assets/data/content/**` are `Cache-Control: no-cache`; `sqlite3.wasm` and `drift_worker.js` remain `public, max-age=2592000`. Latest normal-cache live proof after the N1 lesson-15 deploy confirmed shell/content `no-cache`, wasm `public, max-age=2592000`, and no CDP cache-disabled proof was used. |
| QA-B-002 | Content quality | P2 | Owner-approved kanji expansion: generate JLPT-complete kanji coverage plus 214 Kangxi radicals from open KANJIDIC2 + Unihan, complete meanings/readings/examples/relations, update manifest, and guard against unreachable kanji. | Audit baseline + reachability guard + source policy added; N5/N4 completeness fixed; N3 lessons 02-25 source-verified and live verified; N2 lessons 01-25 source-verified and live verified; N1 lessons 01-25 source-verified and live verified | Added a source-vs-runtime coverage tool and report. Baseline: current unique kanji `638` vs KANJIDIC2 old-JLPT unique `2230`; missing source kanji: N5 `33`, N4 `157`, N2 `654`, N1 `1168`; KANJIDIC2 has no modern N3 tier. Added a runtime guard that fails if any authored kanji asset does not seed into `ContentDatabase` and return from `LessonRepository.fetchKanjiByLevel`, the shared path for grid/search/SRS/reading/writing. Source policy: use KANJIDIC2/Unihan for facts, do not copy unclear-license modern JLPT lists, and choose a redistribution-safe modern mapping before N3/N2/N1 generation. N5, N4, N3, N2, and current N1 incomplete entries are now `0`; lessons 23-25 fixed the last N1 current-entry debt, bumped Kanji seed revision to `70`, deployed, and live-rendered `挑 -> Khiêu` plus `未 -> Vị` in VI/N1 with normal browser cache. Remaining QA-B-002 debt is expansion to JLPT-complete coverage plus radicals after QA-B-001. |
| QA-B-002-L4 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 04 and prove it live. | Fixed + pushed `2683dea2` | Source-verified `甘`, `余`, `編`, `物`, `危`, `怪`, `荒`, `粗`; corrected `物` from wrong `knitting, web` gloss to object/thing meaning; reduced N2 incomplete current entries `176 -> 168`; bumped Kanji seed revision to `30`; full `flutter test` passed (`2340`); deployed and live proof opened `甘` with `Cam (ngọt; dễ dãi; nuông chiều)`, console warnings/errors `0`. |
| QA-B-002-L5 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 05 and prove it live. | Fixed + pushed `c33fb9a1` | Source-verified `争`, `改`, `著`, `有`, `難`, `在`, `安`, `易`; corrected `有`/`難` from generated word-gloss drift; reduced N2 incomplete current entries `168 -> 160`; bumped Kanji seed revision to `31`; full `flutter test` passed (`2340`); deployed and live proof opened `争` with `Tranh (tranh chấp; cạnh tranh; cãi nhau)`, console warnings/errors `0`. |
| QA-B-002-L6 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 06 and prove it live. | Fixed + pushed `4358efd3` | Source-verified `案`, `外`, `言`, `出`, `付`, `意`, `義`, `生`; corrected `外`/`出`/`生` from generated word-gloss drift; reduced N2 incomplete current entries `160 -> 152`; bumped Kanji seed revision to `32`; full `flutter test` passed (`2340`); deployed and live proof opened `案` with `Án (ý tưởng; phương án; vụ việc)`, console warnings/errors `0`. |
| QA-B-002-L7 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 07 and prove it live. | Fixed + pushed `ec6a8a35` | Source-verified `育`, `児`, `幾`, `分`, `花`, `以`, `後`, `降`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `152 -> 144`; bumped Kanji seed revision to `33`; full `flutter test` passed (`2340`); deployed and fresh-IDB live proof opened `育` with `Dục (nuôi dạy; phát triển; giáo dục)`, console warnings/errors `0`. |
| QA-B-002-L8 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 08 and prove it live. | Fixed + pushed `c297667f` | Source-verified `勇`, `衣`, `食`, `住`, `地`, `悪`, `一`, `応`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `144 -> 136`; bumped Kanji seed revision to `34`; focused tests, analyze, string guard, taxonomy/status reports, node research tooling, and full `flutter test` passed (`2340`). Deployed; live VI/N2 proof opened `勇` with `Dũng (dũng cảm; can đảm; khí phách)`, Hán-Việt `Dũng`, on `ユウ`, and kun `いさ.む`. |
| QA-B-002-L9 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 09 and prove it live. | Fixed + pushed `cfe2184e` | Source-verified `段`, `流`, `佚`, `昨`, `日`, `年`, `斉`, `旦`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `136 -> 128`; bumped Kanji seed revision to `35`; focused DB/reachability/taxonomy/upper-JLPT tests, analyze, string guard, taxonomy/status reports, node research tooling, and full `flutter test` passed (`2340`). Deployed; live VI/N2 proof opened `段` with `Đoạn (bậc; đoạn; cấp độ)`, Hán-Việt `Đoạn`, on `ダン, タン`; console warnings/errors `0`. |
| QA-B-002-L10 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 10 and prove it live. | Fixed + pushed `da2242bc` | Source-verified `定`, `移`, `転`, `井`, `緯`, `度`, `従`, `姉`; corrected generated word-gloss fallback drift and `緯` stroke count; reduced N2 incomplete current entries `128 -> 120`; bumped Kanji seed revision to `36`; focused tests, analyze, string guard, taxonomy/status reports, node research tooling, and full `flutter test` passed (`2340`). Deployed; live VI/N2 proof opened `定` with `Định (quyết định; cố định; ổn định)`, Hán-Việt `Định`, on `テイ, ジョウ`, stroke count `8`; console warnings/errors `0`. |
| QA-B-002-L11 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 11 and prove it live. | Fixed + pushed `fadf79fb` | Source-verified `妹`, `威`, `張`, `嫌`, `煎`, `炒`, `入`, `引`; corrected generated word-gloss fallback drift and filled empty `炒`; reduced N2 incomplete current entries `120 -> 112`; bumped Kanji seed revision to `37`; focused tests, analyze, string guard, taxonomy/status reports, node research tooling, and full `flutter test` passed (`2340`). Deployed; live VI/N2 proof opened `妹` with `Muội (em gái; người em nữ)`, Hán-Việt `Muội`, on `マイ`, kun `いもうと`, stroke count `8`; console warnings/errors `0`. |
| QA-B-002-L12 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 12 and prove it live. | Fixed + pushed `ce8ff3a6` | Source-verified `力`, `植`, `木`, `飢`, `浮`, `承`, `受`, `取`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `112 -> 104`; bumped Kanji seed revision to `38`; focused tests, analyze, string guard, taxonomy/status reports, node research tooling, and full `flutter test` passed (`2340`). Deployed; live VI/N2 proof opened `承` with `Thừa (tiếp nhận; thừa nhận; kính nghe)`, Hán-Việt `Thừa`, on `ショウ, ジョウ`, kun `うけたまわ.る, う.ける`, stroke count `8`; console warnings/errors `0`. |
| QA-B-002-L13 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 13 and prove it live. | Fixed + pushed `0601e111` | Source-verified `持`, `薄`, `暗`, `打`, `合`, `消`, `討`, `映`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `104 -> 96`; bumped Kanji seed revision to `39`; focused tests, analyze, string guard, taxonomy/status reports, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; live VI/N2 proof opened `持` with `Trì (cầm; giữ; mang; duy trì)`, Hán-Việt `Trì`, on `ジ`, kun `も.つ, -も.ち, も.てる`, stroke count `9`; console warnings/errors `0`. |
| QA-B-002-L14 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 14 and prove it live. | Fixed + pushed `133d038d` | Source-verified `写`, `無`, `埋`, `敬`, `裏`, `返`, `口`, `占`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `96 -> 88`; bumped Kanji seed revision to `40`; focused tests, analyze, string guard, taxonomy/status reports, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; live VI/N2 proof opened `写` with `Tả (chụp lại; sao chép; phản chiếu)`, Hán-Việt `Tả`, on `シャ, ジャ`, kun `うつ.す, うつ.る, うつ-, うつ.し`, stroke count `5`; console warnings/errors `0`. |
| QA-B-002-L15 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 15 and prove it live. | Fixed + pushed `8d39fa0c` | Source-verified `恨`, `羨`, `売`, `上`, `切`, `行`, `運`, `河`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `88 -> 80`; bumped Kanji seed revision to `41`; focused tests, analyze, string guard, taxonomy/status reports, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; live VI/N2 proof opened `恨` with `Hận (oán hận; thù hằn; nỗi hận)`, Hán-Việt `Hận`, on `コン`, kun `うら.む, うら.めしい`, stroke count `9`; console warnings/errors `0`. |
| QA-B-002-L16 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 16 and prove it live. | Fixed + pushed `1f97e53b` | Source-verified `英`, `文`, `和`, `液`, `体`, `絵`, `具`, `偉`; corrected generated word-gloss fallback drift and resolved learner-facing `液 -> Dịch`; reduced N2 incomplete current entries `80 -> 72`; bumped Kanji seed revision to `42`; focused tests, analyze, string guard, taxonomy/status reports, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; live VI/N2 proof opened `英` with `Anh (Anh; nước Anh; tiếng Anh; ưu tú)`, Hán-Việt `Anh`, on `エイ`, kun `はなぶさ`, stroke count `8`; console warnings/errors `0`. |
| QA-B-002-L17 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 17 and prove it live. | Fixed + pushed `88aca79a` | Source-verified `宴`, `会`, `園`, `芸`, `演`, `劇`, `円`, `周`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `72 -> 64`; bumped Kanji seed revision to `43`; focused tests, analyze, string guard, taxonomy/status reports, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; live VI/N2 proof opened `宴` with `Yến (yến; tiệc; yến tiệc; tiệc rượu)`, Hán-Việt `Yến`, on `エン`, kun `うたげ`, stroke count `10`; console warnings/errors `0`. |
| QA-B-002-L18 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 18 and prove it live. | Fixed + pushed `1dc4252f` | Source-verified `遠`, `延`, `長`, `煙`, `突`, `追`, `掛`, `越`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `64 -> 56`; bumped Kanji seed revision to `44`; focused tests, analyze, string guard, taxonomy/status reports, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; live VI/N2 proof opened `遠` with `Viễn (viễn; xa; xa xôi; cách xa)`, Hán-Việt `Viễn`, on `エン, オン`, kun `とお.い`, stroke count `13`; console warnings/errors `0`. |
| QA-B-002-L19 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 19 and prove it live. | Fixed + pushed `6e758b11` | Source-verified `援`, `王`, `女`, `接`, `対`, `往`, `復`, `欧`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `56 -> 48`; bumped Kanji seed revision to `45`; focused tests, analyze, string guard, taxonomy/status reports, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; live VI/N2 proof opened `援` with `Viện (viện; hỗ trợ; viện trợ; cứu giúp)`, Hán-Việt `Viện`, on `エン`, stroke count `12`; console warnings/errors `0`. |
| QA-B-002-L20 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 20 and prove it live. | Fixed + pushed `1ed515fd` | Source-verified `米`, `用`, `大`, `通`, `凡`, `帰`, `拝`, `代`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `48 -> 40`; bumped Kanji seed revision to `46`; focused tests, analyze, string guard, taxonomy/status reports, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; live VI/N2 proof opened `米` with `Mễ (mễ; gạo; lúa gạo; Hoa Kỳ)`, Hán-Việt `Mễ`, on `ベイ, マイ, メエトル`, kun `こめ, よね`, stroke count `6`; console warnings/errors `0`. |
| QA-B-002-L21 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 21 and prove it live. | Fixed + pushed `3de785e8` | Source-verified `補`, `屋`, `送`, `仮`, `怠`, `押`, `納`, `治`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `40 -> 32`; bumped Kanji seed revision to `47`; focused tests, analyze, string guard, taxonomy/status reports, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; live VI/N2 production proof fetched deployed `補` metadata with `Bổ (bổ sung; bù đắp; hỗ trợ)`, Hán-Việt `Bổ`, on `ホ`, kun `おぎな.う`, stroke count `12`; console warnings/errors `0`. |
| QA-B-002-L24 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 24 and prove it live with normal-cache headers. | Fixed + pushed `af3776a4` | Source-verified `母`, `参`, `思`, `込`, `重`, `親`, `指`, `卸`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `16 -> 8`; bumped Kanji seed revision to `50`; focused DB/reachability/taxonomy/upper-JLPT tests passed and release web build passed. Deployed; normal-cache live proof fetched deployed `lesson_24.json` with `Cache-Control: no-cache` and `母` metadata `Mẫu (mẹ; mẫu thân; bậc nữ lớn tuổi)`, Hán-Việt `Mẫu`, on `ボ`, kun `はは/も`, stroke count `5`; console warnings/errors `0`. |
| QA-B-002-L25 | Content quality | P2 | Continue QA-B-002: source-verify N2 lesson 25 and prove it live with normal-cache headers. | Fixed + pushed `3448965e` | Source-verified `恩`, `恵`, `温`, `室`, `泉`, `帯`, `中`, `人`; corrected generated word-gloss fallback drift; reduced N2 incomplete current entries `8 -> 0`; bumped Kanji seed revision to `51`; focused tests, analyze, string guard, content status, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; normal-cache live proof fetched deployed `lesson_25.json` with `Cache-Control: no-cache` and `恩` metadata `Ân (ơn nghĩa; lòng tốt; ân huệ)`, Hán-Việt `Ân`, on `オン`, stroke count `10`; console warnings/errors `0`. |
| QA-B-002-N1-L1 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 1 and prove it live with normal-cache headers. | Fixed + pushed `dbc11327` | Source-verified `嗚`, `呼`, `相`, `変`, `愛`, `想`, `対`, `間`; added readings/related kanji and truthful `vi-source-verified`; reduced N1 incomplete current entries `200 -> 192`; bumped Kanji seed revision to `52`; focused tests, analyze, string guard, content status, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; normal-cache live proof fetched deployed `lesson_01.json` with `Cache-Control: no-cache` and `嗚` metadata `Ô (than khóc; tiếng kêu than; chao ôi)`, Hán-Việt `Ô`, on `ウ/オ`, kun `ああ`, stroke count `13`; console warnings/errors `0`. |
| QA-B-002-N1-L2 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 2 and prove it live with normal-cache headers. | Fixed + pushed `5a9620ac` | Source-verified `柄`, `憎`, `合`, `曖`, `昧`, `敢`, `仰`, `垢`; corrected generated gloss/readings/strokes, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `192 -> 184`; bumped Kanji seed revision to `53`; focused tests, analyze, string guard, content status, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; normal-cache live proof fetched deployed `lesson_02.json` with `Cache-Control: no-cache` and `柄` metadata `Bính (hoa văn; tính chất; tay cầm)`, Hán-Việt `Bính`, on `ヘイ`, kun `がら/え/つか`, stroke count `9`. |
| QA-B-002-N1-L3 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 3 and prove it live with normal-cache headers. | Fixed + pushed `8610360c` | Source-verified `亜`, `科`, `銅`, `証`, `赤`, `字`, `明`, `白`; corrected generated word-level readings/glosses, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `184 -> 176`; bumped Kanji seed revision to `54`; focused tests, analyze, string guard, content status, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; normal-cache live proof fetched deployed `lesson_03.json` with `Cache-Control: no-cache` and `亜` metadata `Á (châu Á; thứ hai; phụ/á)`, Hán-Việt `Á`, on `ア`, kun `つ.ぐ`, stroke count `7`; console warnings/errors `0`. |
| QA-B-002-N1-L4 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 4 and prove it live with normal-cache headers. | Fixed + pushed `b27a84cd` | Source-verified `上`, `商`, `人`, `空`, `諦`, `呆`, `悪`, `灰`; corrected generated word-level readings/glosses, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `176 -> 168`; bumped Kanji seed revision to `55`; focused tests, analyze, string guard, content status, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; normal-cache live proof fetched deployed `lesson_04.json` with `Cache-Control: no-cache` and `上` metadata `Thượng (trên; lên; tăng)`, Hán-Việt `Thượng`, on `ジョウ/ショウ/シャン`, stroke count `3`; console warnings/errors `0`. |
| QA-B-002-N1-L5 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 5 and prove it live with normal-cache headers. | Fixed + pushed `69a404f9` | Source-verified `日`, `憧`, `顎`, `麻`, `後`, `朝`, `寝`, `坊`; replaced word-level readings with source-backed Kanji readings, removed old approval metadata, added truthful `vi-source-verified`, and replaced suspicious `悪日/あくび` example with `明後日/あさって`; reduced N1 incomplete current entries `168 -> 160`; bumped Kanji seed revision to `56`; focused tests, analyze, string guard, content status, node research tooling, full `flutter test` (`2340`), and release web build passed. Deployed; normal-cache live proof fetched deployed `lesson_05.json` with `Cache-Control: no-cache` and `日` metadata `Nhật (ngày; mặt trời; Nhật Bản)`, Hán-Việt `Nhật`, on `ニチ/ジツ`, kun `ひ/-び/-か`, stroke count `4`; console warnings/errors `0`. |
| QA-B-002-N1-L6 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 6 and prove it live with normal-cache headers. | Fixed + pushed `95534f80` | Source-verified `浅`, `欺`, `鮮`, `笑`, `味`, `東`, `焦`, `彼`; corrected `浅` stroke count, replaced word-level readings with source-backed Kanji readings, normalized `東` to learner-facing `Đông`, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `160 -> 152`; bumped Kanji seed revision to `57`; focused DB/reachability/taxonomy/upper-JLPT tests and `flutter analyze lib test` passed. Deployed; normal-cache live proof fetched deployed `lesson_06.json` with `Cache-Control: no-cache` and `浅` metadata `Thiển (nông; cạn; hời hợt)`, on `セン`, kun `あさ.い`, stroke count `9`; console warnings/errors `0`. |
| QA-B-002-N1-L7 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 7 and prove it live with normal-cache headers. | Fixed + pushed `191e6db2` | Source-verified `処`, `値`, `私`, `当`, `前`, `他`, `方`, `此`; replaced word-level readings with source-backed Kanji readings, replaced awkward examples (`彼処`, `彼方此方`) with cleaner examples (`処理`, `方法`), corrected `他人` reading to `たにん`, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `152 -> 144`; bumped Kanji seed revision to `58`; focused tests, analyze, string guard, content status, and full `flutter test` (`2340`) passed. Deployed; normal-cache live proof fetched deployed `lesson_07.json` with `Cache-Control: no-cache` and `処` metadata `Xử (xử lý; giải quyết; nơi chốn)`, on `ショ`, kun `ところ/-こ/お.る`, stroke count `5`; console warnings/errors `0`. |
| QA-B-002-N1-L8 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 8 and prove it live with normal-cache headers. | Fixed + pushed `57cdbac4` | Source-verified `化`, `気`, `口`, `圧`, `迫`, `扱`, `集`, `誂`; replaced word-level readings with source-backed Kanji readings, aligned duplicate `化`/`口`/`圧` with verified rows, corrected `誂` away from misleading English `tempt`, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `144 -> 136`; bumped Kanji seed revision to `59`; focused tests, analyze, string guard, content status, and full `flutter test` (`2340`) passed. Deployed; normal-cache live proof fetched deployed `lesson_08.json` with `Cache-Control: no-cache` and `化` metadata `Hóa (biến đổi; biến hóa; -hóa)`, on `カ/ケ`, kun `ば.ける/ば.かす/ふ.ける/け.する`, stroke count `4`; console warnings/errors `0`. |
| QA-B-002-N1-L9 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 9 and prove it live with normal-cache headers. | Fixed + pushed `e35ae6ac` | Source-verified `力`, `宛`, `跡`, `継`, `回`, `貴`, `女`, `溢`; replaced word-level readings with source-backed Kanji readings, corrected `女` learner-facing Hán-Việt from `Nữa` to `Nữ`, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `136 -> 128`; bumped Kanji seed revision to `60`; hosting cache guard, focused tests, analyze, string guard, content status, and full `flutter test` (`2340`) passed. Deployed; normal-cache live proof fetched deployed `lesson_09.json` with `Cache-Control: no-cache` and `力` metadata `Lực (sức mạnh; lực; năng lực)`, on `リョク/リキ/リイ`, kun `ちから`; console warnings/errors `0`. |
| QA-B-002-N1-L10 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 10 and prove it live with normal-cache headers. | Fixed + pushed `ac75b039` | Source-verified `油`, `絵`, `炙`, `甘`, `雨`, `具`, `天`, `網`; replaced word-level readings with source-backed Kanji readings, normalized `油` to learner-facing `Du`, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `128 -> 120`; bumped Kanji seed revision to `61`; focused tests, analyze, string guard, content status, hosting cache guard, and full `flutter test` (`2340`) passed. Deployed; normal-cache live proof fetched deployed `lesson_10.json` with `Cache-Control: no-cache` and `油` metadata `Du (dầu; chất béo; sơn dầu)`, on `ユ/ユウ`, kun `あぶら`; console warnings/errors `0`. |
| QA-B-002-N1-L11 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 11 and prove it live with normal-cache headers. | Fixed + pushed `f04728a9` | Source-verified `地`, `操`, `危`, `過`, `誤`, `歩`, `予`, `荒`; replaced word-level readings with source-backed Kanji readings, corrected learner-facing `予` from generated `Nhừ` to `Dự`, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `120 -> 112`; bumped Kanji seed revision to `62`; focused tests, analyze, string guard, content status, hosting cache guard, and full `flutter test` (`2340`) passed. Deployed; normal-cache live proof fetched deployed `lesson_11.json` with `Cache-Control: no-cache` and `予` metadata `Dự (trước; dự tính; chuẩn bị)`, on `ヨ/シャ`, kun `あらかじ.め`; console warnings/errors `0`. |
| QA-B-002-N1-L12 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 12 and prove it live with normal-cache headers. | Fixed + pushed `13164fac` | Source-verified `粗`, `筋`, `争`, `改`, `凡`, `現`, `有`, `難`; replaced word-level readings with source-backed Kanji readings, corrected `筋` from vernacular `Gân` to learner-facing `Cân`, replaced ateji `有難う` examples with clearer `有無`/`困難`, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `112 -> 104`; bumped Kanji seed revision to `63`; focused tests, analyze, string guard, content status, hosting cache guard, and full `flutter test` (`2340`) passed. Deployed; normal-cache live proof fetched deployed `lesson_12.json` with `Cache-Control: no-cache` and `粗` metadata `Thô (thô; sơ sài; thô ráp)`, on `ソ`, kun `あら.い/あら-`; console warnings/errors `0`. |
| QA-B-002-N1-L13 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 13 and prove it live with normal-cache headers. | Fixed + pushed `f66f53e0` | Source-verified `様`, `或`, `慌`, `暗`, `殺`, `算`, `示`, `案`; replaced word-level readings with source-backed Kanji readings, corrected `示` from generated `Kì Thị` to learner-facing `Thị`, corrected `案` from generated `An Án Yên` to `Án`, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `104 -> 96`; bumped Kanji seed revision to `64`; focused tests, analyze, string guard, content status, hosting cache guard, and full `flutter test` (`2340`) passed. Deployed; normal-cache live proof fetched deployed `lesson_13.json` with `Cache-Control: no-cache` and `示` metadata `Thị (chỉ ra; biểu thị; cho thấy)`, on `ジ/シ`, kun `しめ.す`; console warnings/errors `0`. |
| QA-B-002-N1-L14 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 14 and prove it live with normal-cache headers. | Fixed + pushed `66694ebe` | Source-verified `安`, `静`, `定`, `余`, `依`, `良`, `伊`, `井`; replaced word-level readings with source-backed Kanji readings, replaced suspicious `伊井/いい` examples with `伊豆/いず` and `井戸/いど`, removed old approval metadata including `unihan-kanji-checked`, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `96 -> 88`; bumped Kanji seed revision to `65`; focused tests, analyze, string guard, content status, hosting cache guard, and full `flutter test` (`2340`) passed. Deployed; normal-cache live proof fetched deployed `lesson_14.json` with `Cache-Control: no-cache` and `伊` metadata `Y (Ý; người ấy; dùng trong tên riêng)`, on `イ`, kun `かれ`; old approval tags absent; console warnings/errors `0`. |
| QA-B-002-N1-L15 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 15 and prove it live with normal-cache headers. | Fixed + pushed `18e84e51` | Source-verified `否`, `加`, `減`, `言`, `訳`, `家`, `出`, `主`; replaced word-level readings with source-backed Kanji readings, corrected learner-facing `否 -> Phủ` and `主 -> Chủ`, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `88 -> 80`; bumped Kanji seed revision to `66`; focused DB/reachability/taxonomy/upper-JLPT tests (`45`) and hosting cache guard passed. Deployed; normal-cache live proof fetched deployed `lesson_15.json` with `Cache-Control: no-cache` and `否` metadata `Phủ (không; phủ định; từ chối)`, on `ヒ`, kun `いな/いや`; old approval tags absent; console warnings/errors `0`. |
| QA-B-002-N1-L16 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 16 and prove it live with normal-cache headers. | Fixed + pushed `b124248f` | Source-verified `如`, `何`, `生`, `雷`, `怒`, `歪`, `粋`, `域`; replaced word-level readings with source-backed Kanji readings, corrected learner-facing `歪 -> Oai`, replaced `域外` with clearer `領域`, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `80 -> 72`; bumped Kanji seed revision to `67`; focused tests, analyze, string guard, content status, and hosting cache guard passed. Deployed; normal-cache live proof fetched deployed `lesson_16.json` with `Cache-Control: no-cache` and `歪` metadata `Oai (méo; lệch; vặn vẹo)`, on `ワイ/エ`, kun `いが.む/いびつ/ひず.む/ゆが.む`; old approval tags absent; console warnings/errors `0`. |
| QA-B-002-N1-L17 | Content quality | P2 | Continue QA-B-002: source-verify N1 lesson 17 and prove it live with normal-cache headers. | Fixed + pushed `058501e2` | Source-verified `外`, `意`, `込`, `経`, `緯`, `行`, `違`, `成`; replaced word-level readings with source-backed Kanji readings, corrected `緯` stroke count `15 -> 16`, kept learner-facing `行 -> Hành`, replaced ateji `行き成り` with clearer `達成`, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `72 -> 64`; bumped Kanji seed revision to `68`; focused tests, analyze, string guard, content status, and hosting cache guard passed. Deployed; normal-cache live proof fetched deployed `lesson_17.json` with `Cache-Control: no-cache` and `緯` metadata `Vĩ (vĩ tuyến; sợi ngang; chiều ngang)`, stroke count `16`; old approval tags absent; console warnings/errors `0`. |
| QA-B-002-N1-L18-L22 | Content quality | P2 | Continue QA-B-002 under owner batch policy: source-verify N1 lessons 18-22 and prove rendered UI with normal browser cache. | Fixed + deployed | Source-verified forty kanji (`異` through `面`), replaced word-level readings with KANJIDIC2 readings, corrected `移 -> Di`, `別 -> Biệt`, `碑` stroke `13 -> 14`, `概` stroke `13 -> 14`, `部` stroke `10 -> 11`, removed old approval metadata, and added truthful `vi-source-verified`; reduced N1 incomplete current entries `64 -> 24`; bumped Kanji seed revision to `69`; focused tests, analyze, string guard, content status, hosting cache guard, full `flutter test` (`2340`), and release build passed. Deployed; normal-cache live proof rendered the actual VI/N1 Kanji UI: search `議` showed `Hán-Việt Nghị`, detail modal showed `Nghị (bàn luận; nghị sự; ý kiến)`, search `別` showed `Hán-Việt Biệt` and not stale `Biết`; page-context fetch returned content/shell `Cache-Control: no-cache`, wasm/worker `public, max-age=2592000`, console warnings/errors `0`. |
| QA-B-001 | Content quality | P1 | Owner-delegated mega-loop: verify Vietnamese content across N5-N1 with source-backed per-item method and `vi-source-verified` taxonomy. | Pending | Not started in this session; Track A P0/P1 live blockers first. |

## 2026-05-19 Addendum

| ID | Track | Severity | Repro / Detection | Status | Verification |
| --- | --- | --- | --- | --- | --- |
| QA-A-018 | App defects | P1 | Owner Firebase Console check on 2026-05-20 showed Authentication App Check at 0% verified / 100% unverified. Local live Network proof showed no `firebaseappcheck.googleapis.com`, no reCAPTCHA/App Check SDK request, and Auth requests without `X-Firebase-AppCheck`; root cause is deployable web builds could miss App Check activation because `firebase-app-check.js` was not preloaded and local deploys were not forced through a key-guarded build. | Fixed + deployed | Added RED/GREEN guard for App Check SDK preload, deploy-helper key requirement/dart-defines, non-silent release logging, and CI deploy-helper use. Deployed via `node tool/deploy/hosting_deploy.js`. Live proof: App Check SDK loaded, reCAPTCHA requested, `firebaseappcheck.googleapis.com` exchange attempted, Auth requests carried `X-Firebase-AppCheck`, Sentry CDN returned `200`. Headless reCAPTCHA exchange returned `403`, so enforcement stays off until owner sees healthy verified traffic for 1-2 weeks. |
| QA-A-019 | App defects | P0 | Directive D live proof found upper-level grammar details with empty examples because N1/N2 `grammar_examples` files use object schema `{ "examples": [...] }` and flat example rows, while seeders only decoded list/block schemas. | Fixed + deployed | Seeders now decode both list and object schemas, flat matching rows seed examples, and an integrity guard requires every N5-N1 grammar definition to have matching examples. Live proof: N2 direct detail `/#/grammar/1` rendered four examples and `Luyện tập để hiểu` opened `Câu 1/5`. |
| QA-A-020 | App defects | P1 | Directive D live proof found Sentry web SDK could be blocked by Hosting CSP because `https://browser.sentry-cdn.com` was absent from `script-src`. | Fixed + deployed | CSP now includes Sentry CDN in `script-src` and keeps `https://*.sentry.io` in `connect-src`; node regression covers the workflow/helper and CSP. Live proof: Sentry CDN response status `200`, no CSP block. |
| QA-A-021 | App defects | P1 | Directive D live proof found direct grammar detail links could stay loading or show not found on a fresh browser because `getGrammarDetail(id)` read DB before seeding the active level. | Fixed + deployed | Added RED regression for N4 direct deep link `/#/grammar/81`, then seeded the active grammar level before a missing detail lookup. Live proof: fresh VI/N4 direct `/#/grammar/81` rendered examples and `Luyện tập để hiểu` opened `Câu 1/5`; N4 search for `ところです` returned the point, not empty state. |
| QA-B-001-G-N2-L01-L05 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N2 grammar lessons 1-5 and prove the edited grammar renders in the live app. First live proof found stale `Verb-stem + ことなく` because Grammar UI uses `AppDatabase`/`GrammarSeeder`, not `ContentDatabase`. | Fixed + deployed | Added `vi-source-verified` to 38 points, corrected `ことなく` to `Verb-dictionary form + ことなく`, bumped `GrammarSeeder` revision to `12`, made `fetchPointsByLevel` invoke versioned reseed, and fixed seed-key false matches. Focused suite `68/68`, analyze clean, content status machine/open-review `0`, full `flutter test` `2343/2343`, release build/deploy passed. Existing production browser with normal cache upgraded N2 grammar version to `12` and rendered the corrected `ことなく` detail; console warnings/errors `0`. |
| QA-B-001-G-N2-L06-L10 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N2 grammar lessons 6-10 and prove the edited grammar renders in the live app. | Fixed + deployed | Added `vi-source-verified` to 38 points, corrected `っこない` to `Verb-ます stem + っこない`, bumped grammar seed revisions to `13`. JSON/tag check `38/38`, focused suite `62/62`, analyze clean, UI string guard `0`, content status machine/open-review `0`, release build/deploy passed. Existing production browser with normal cache upgraded N2 grammar version to `13` and rendered the corrected `っこない` detail; console warnings/errors `0`. |
| QA-B-001-G-N2-L11-L15 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N2 grammar lessons 11-15 and prove the edited grammar renders in the live app. | Fixed + deployed | Added `vi-source-verified` to 38 points, corrected `っぽい`, `てかなわない`, `とおり`, `ないことはない`, `ないこともない`, `ながら`, `にあたり`, and `にしたがって`; bumped grammar seed revisions to `14`. JSON/tag check `38/38`, focused suite `62/62`, analyze clean, UI string guard `0`, content status machine/open-review `0`, release build/deploy passed. Existing production browser with normal cache upgraded N2 grammar version to `14` and rendered the corrected `ながら` detail; console warnings/errors `0`. |
| QA-B-001-G-N2-L16-L20 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N2 grammar lessons 16-20 and prove the edited grammar renders in the live app. | Fixed + deployed | Added `vi-source-verified` to 38 points, corrected `にともなって`, `にほかならない`, `により`, `に決まっている`, `に関わって`, `に際して`, `の下で`, `ばかりだ`, `ば～というものでもない`, `ままに`, and `もかまわず`; bumped grammar seed revisions to `15`. JSON/source-tag check `38/38`, focused suite `50/50`, analyze clean, literal-route guard clean, content status machine/open-review `0`, release build/deploy passed. Existing production browser with normal cache rendered the corrected `ばかりだ` list row and detail; shell/content returned `Cache-Control: no-cache`; console warnings/errors `0`. |
| QA-B-001-G-N2-L21-L25 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N2 grammar lessons 21-25 and prove the edited grammar renders in the live app. | Fixed + deployed | Added `vi-source-verified` to 39 points, corrected/clarified `ものなら`, `も同然だ`, `わけがない`, `わけだ`, `わけではない`, `わけにはいかない`, `上で`, `上は`, `以上`, `以来`, `恐れがある`, `末`, `次第です`, `気味`, and `際に`; bumped grammar seed revisions to `16`. JSON/source-tag check `39/39`, focused suite `32/32`, analyze clean, literal-route guard clean, content status machine/open-review `0`, full `flutter test` `2343/2343`, release build/deploy passed. Existing production browser with normal cache rendered corrected `上で` and `末` detail screens; shell/content returned `Cache-Control: no-cache`; console warnings/errors `0`. |
| QA-B-001-G-N3-L01-L05 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N3 grammar lessons 1-5 and prove the edited grammar renders in the live app. | Fixed + deployed | Added `vi-source-verified` to 20 points, corrected/clarified `代わりに`, `わりに`, `うちに`, `間に`, `はずだ`, `わけではない`, `わけにはいかない`, and `はずがない`; bumped grammar seed revisions to `17`. JSON/source-tag check `20/20`, focused suite `32/32`, analyze clean, literal-route guard clean, content status machine/open-review `0`, release build/deploy passed. Existing production browser with normal cache rendered corrected N3 `代わりに` and `わけにはいかない` detail screens; shell/content returned `Cache-Control: no-cache`; console warnings/errors `0`. Full `flutter test` deferred until N3 grammar level completion. |
| QA-B-001-G-N3-L06-L10 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N3 grammar lessons 6-10 and prove the edited grammar renders in the live app. | Fixed + deployed | Added `vi-source-verified` to 20 points, corrected/clarified `すぎる`, `かえって`, `おそれがある`, `に違いない`, `ようだ`, `らしい`, `みたいだ`, `ように見える`, and `そうだ（伝聞）`; bumped grammar seed revisions to `19`. JSON/source-tag check `20/20`, focused suite `63/63`, analyze clean, UI string guard clean, literal-route guard clean, content status machine/open-review `0`, release build/deploy passed. Existing production browser with normal cache upgraded N3 grammar version to `19`; rendered detail screens for `すぎる` and `かえって` with corrected `KẾT NỐI`; shell/content returned `Cache-Control: no-cache`; console warnings/errors `0`. Full `flutter test` deferred until N3 grammar level completion. |
| QA-B-001-G-N3-L11-L15 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N3 grammar lessons 11-15 and prove the edited grammar renders in the live app. | Fixed + deployed | Added `vi-source-verified` to 20 points, corrected/clarified `せいで`, `おかげで`, `ため`, `可能性がある`, `ほど`, `くらい/ぐらい`, `さ`, `ばかりか`, `ことがある`, `ないことはない`, `ないこともない`, `といい`, `ばよかった`, and `といいな`; bumped grammar seed revisions to `20`. JSON/source-tag check `20/20`, focused suite `63/63`, analyze clean, UI string guard clean, literal-route guard clean, content status machine/open-review `0`, release build/deploy passed. Existing production browser with normal cache upgraded N3 grammar version to `20`; rendered detail screens for `せいで` and `ないことはない` with corrected `KẾT NỐI`; shell/content returned `Cache-Control: no-cache`; console warnings/errors `0`. Full `flutter test` deferred until N3 grammar level completion. |
| QA-B-001-G-N3-L16-L20 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N3 grammar lessons 16-20 and prove the edited grammar renders in the live app. | Fixed + deployed | Added `vi-source-verified` to 20 points, corrected/clarified `続ける`, `きる`, `抜く`, `という`, `といわれている`, `ことから`, `とされている`, `べきだ`, `べきではない`, `たとたん`, `ついでに`, `際に`, `気がする`, `ものだ`, `わけだ`, and `に決まっている`; bumped grammar seed revisions to `21`. JSON/source-tag check `20/20`, focused suite `63/63`, analyze clean, UI string guard clean, literal-route guard clean, content status machine/open-review `0`, release build/deploy passed. Existing production browser with normal cache upgraded N3 grammar version to `21`; rendered detail screens for `べきだ` and `わけだ` with corrected `KẾT NỐI`; shell/content returned `Cache-Control: no-cache`; console warnings/errors `0`. Full `flutter test` deferred until N3 grammar level completion. |
| QA-B-001-G-N3-L21-L25 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N3 grammar lessons 21-25 and prove the edited grammar renders in the live app. | Fixed + deployed | Added `vi-source-verified` to 20 points, corrected/clarified `つつある`, `にかけて`, `ほど〜ない`, `というより`, `ばかりでなく`, and `どんなに〜ても`; bumped grammar seed revisions to `22`, completing N3 grammar. JSON/source-tag check `20/20`, focused suite passed, analyze clean, UI string guard clean, literal-route guard clean, content status machine/open-review `0`, full `flutter test` `2343/2343`, release build/deploy passed. Production browser with normal cache upgraded N3 grammar version to `22`; rendered corrected rows for `にかけて`, `ほど〜ない`, `ばかりでなく`, and `どんなに〜ても`, plus detail for `ばかりでなく`; shell/content returned `Cache-Control: no-cache`; console warnings/errors `0`. |
| QA-B-001-G-N4-L26-L30 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N4 grammar lessons 26-30 and prove the edited grammar renders in the live app. | Fixed + deployed | Replaced old `vi-human-approved` tags with truthful `vi-source-verified` on 20 points; corrected/clarified `んです`, `いただけませんか`, `たらいいですか`, `んですが`, `しか〜ない`, `ながら`, `し`, result-state `ている`, `てしまう/ちゃう`, `てある`, and `ておく`; bumped grammar seed revisions to `24` after live proof caught canonicalization stripping `なA/N` notes. JSON/source-tag check `20/20`, banned-tag check clean, focused suite passed, analyze clean, UI string guard clean, literal-route guard clean, content status machine/open-review `0`, release build/deploy passed. Production browser with normal cache rendered N4 grammar version `24`, corrected rows for `んです`, `し`, `てある`, and detail for `Nが + 他動詞て形 + あります`; shell/content returned `Cache-Control: no-cache`; console warnings/errors `0`. Full `flutter test` deferred until N4 grammar level completion. |
| QA-B-001-G-N4-L31-L35 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N4 grammar lessons 31-35 and prove the edited grammar renders in the live app. | Fixed + deployed | Replaced old `vi-human-approved` tags with truthful `vi-source-verified` on 20 points; corrected/clarified `つもり`, `予定`, `ほうがいい`, `でしょう`, `かもしれません`, `でしょうか`, imperative/prohibitive, reading/meaning/reporting quote patterns, `ば`, and `ても`; bumped grammar seed revisions to `25`. JSON/source-tag check `20/20`, banned-tag check clean, focused suite passed, analyze clean, UI string guard clean, literal-route guard clean, content status machine/open-review `0`, release build/deploy passed. Production browser with normal cache rendered N4 grammar version `25`, corrected rows for `でしょう`, `かもしれません`, `ば`, `ても`, and detail for `ても`; shell/content returned `Cache-Control: no-cache`; console warnings/errors `0`. Full `flutter test` deferred until N4 grammar level completion. |
| QA-B-001-G-N4-L36-L40 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N4 grammar lessons 36-40 and prove the edited grammar renders in the live app. | Fixed + deployed | Replaced old `vi-human-approved` tags with truthful `vi-source-verified` on 20 points; corrected/clarified `ように`, `ようになりました`, `ようにしています`, passive forms, nominalization with `の`, cause `て`, `ので`, `ために`, `おかげで`, embedded questions, `かどうか`, and `てみます`; bumped grammar seed revisions to `26`. JSON/source-tag check `20/20`, banned-tag check clean, focused suite `48/48`, analyze clean, UI string guard clean, literal-route guard clean, content status machine/open-review `0`, release build/deploy passed. Production browser with normal cache rendered N4 grammar version `26`, corrected `かどうか` rows and detail `KẾT NỐI: V普通形 / いA / なA語幹 / N + かどうか`; shell/content returned `Cache-Control: no-cache`; console warnings/errors `0`. Full `flutter test` deferred until N4 grammar level completion. |
| QA-B-001-G-N4-L41-L45 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N4 grammar lessons 41-45 and prove the edited grammar renders in the live app. | Fixed + deployed | Replaced old `vi-human-approved` tags with truthful `vi-source-verified` on 20 points; corrected/clarified giving/honorific receiving patterns, purpose `ために`, usage `のに`, appearance `そうだ`, `てくる/ていく`, `すぎます`, `やすい/にくい`, state-change `く/にする・なる`, `場合は`, contrast `のに`, `とき`, and `ても`; bumped grammar seed revisions to `28`. JSON/source-tag check `20/20`, banned-tag check clean, focused suite `48/48`, analyze clean, UI string guard clean, literal-route guard clean, content status machine/open-review `0`, release build/deploy passed. Production browser with normal cache rendered N4 grammar version `28`, corrected `すぎます` row and detail `KẾT NỐI: Vます語幹 / いA語幹 / なA語幹 + すぎます`; shell/content returned `Cache-Control: no-cache`; console warnings/errors `0`. Full `flutter test` deferred until N4 grammar level completion. |
| QA-B-001-G-N4-L46-L50 | Content quality | P1 | QA-B-001 grammar-first batch: source-verify N4 grammar lessons 46-50 and prove the edited grammar renders in the live app. Directive D also required checking examples, direct details, search, and practice gate for the same grammar surface. | Fixed + deployed | Replaced old `vi-human-approved` tags with truthful `vi-source-verified` on 20 points; corrected/clarified `ところです`, `はずです`, hearsay `そうです`, `ようです`, `らしい`, `みたいです`, causative, honorific, humble, and `ございます`/`でございます`; bumped grammar seed revisions to `29`; fixed direct detail lazy seed. Verified: research tooling `61`, UI string guard `0`, content status machine/open-review `0`, `flutter analyze lib test`, focused Flutter suite `72`, full `flutter test` `2347`, release build/deploy, web resource smoke, and live VI/N4 proof for direct detail/examples/practice/search. |
| QA-C-001 | Conjugation feature | P1 | Owner request: add Japanese verb/adjective conjugation learning under Grammar, sourced from JMdict POS tags, with a pure Dart conjugation engine, TDD eval-first fixtures, vocab-detail conjugation panels, Grammar routes, drills, and unified SRS updates for conjugation skill + vocab + kanji. | Phase 0 pending | Before implementation, write `docs/research/conjugation-feature-design-2026-05-19.md` covering pedagogy/source research, app/schema/SRS audit, IA, data model, engine/test plan, forms coverage, drill model, cross-links, and implementation slices. Do not add `vi-human-approved`. |
